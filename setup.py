from setuptools import setup, find_packages

setup(
    name="multilingual_rag_assistant",
    version="0.0.1",
    author="Aditya",
    packages=["backend", "data", "frontend"],
    install_requires=[],
)